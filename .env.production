# Production Environment Configuration
# Copy this to .env.production and update values for your AWS deployment

# AWS Configuration
AWS_REGION=us-east-1
NODE_ENV=production
ENVIRONMENT=production
IS_LOCAL=false

# DynamoDB - Environment-aware table names
# Tables are automatically prefixed based on ENVIRONMENT
# Production: construction-expenses-{table-suffix}
# Staging: construction-expenses-staging-{table-suffix}
# Dev: construction-expenses-dev-{table-suffix}
TABLE_PREFIX=construction-expenses

# Legacy - deprecated in favor of environment-aware table-config.js
TABLE_NAME=construction-expenses-production-table

# Stack Configuration
STACK_NAME=construction-expenses-production

# Clerk Configuration
# SECURITY: API keys now stored in AWS Secrets Manager
# Keys are fetched automatically by Lambda functions
CLERK_AUTH_ENABLED=true
CLERK_FRONTEND_API=https://Builder-expenses.clerk.accounts.dev

# Sentry Configuration
# SECURITY: DSN now stored in AWS Secrets Manager
# DSN is fetched automatically by Lambda functions
SENTRY_TRACES_SAMPLE_RATE=1.0
SENTRY_ENVIRONMENT=production

# Paddle Configuration - PRODUCTION
# SECURITY: ALL secrets now stored in AWS Secrets Manager
#   - construction-expenses/paddle/api-key
#   - construction-expenses/paddle/webhook-secret
#   - construction-expenses/paddle/client-token
# Secrets are fetched automatically by Lambda functions
PADDLE_ENVIRONMENT=production

# Paddle Production Price IDs (updated 2026-01-01)
# Pricing: Starter ₪50, Professional ₪100, Enterprise ₪150
# These are now configurable via environment variables
# If not set, defaults to production IDs in paddle-utils.js
PADDLE_STARTER_PRICE_ID=pri_01kdwqn0d0ebbev71xa0v6e2hd
PADDLE_PROFESSIONAL_PRICE_ID=pri_01kdwqsgm7mcr7myg3cxnrxt9y
PADDLE_ENTERPRISE_PRICE_ID=pri_01kdwqwn1e1z4xc93rgstytpj1

# Paddle Product IDs (optional - for complete configuration)
PADDLE_STARTER_PRODUCT_ID=pro_01kdwf11hqt0w7anyyq7chr23a
PADDLE_PROFESSIONAL_PRODUCT_ID=pro_01kdwqqf1cg364eztm3n0x4vg3
PADDLE_ENTERPRISE_PRODUCT_ID=pro_01kdwqvfkg3sajqszq4x0wfjbq

# Frontend URL for Paddle redirects
FRONTEND_URL=https://builder-expenses.com
CLOUDFRONT_DISTRIBUTION_ID=E3EYFZ54GJKVNL

# Textract Configuration (DEPRECATED - replaced by Claude OCR)
# AWS Textract for Smart Expense OCR
# TEXTRACT_REGION=us-east-1
# TEXTRACT_ENABLED=false
# Cost: $0.017 per page, ~$51/month for 3,000 receipts

# OpenRouter Configuration
# Claude 3.5 Sonnet via OpenRouter for Smart Receipt OCR
# SECURITY: Store in AWS Secrets Manager: construction-expenses/openrouter-api-key
# For local testing only:
# Cost: ~$0.007 per receipt, ~$7/month for 1,000 receipts